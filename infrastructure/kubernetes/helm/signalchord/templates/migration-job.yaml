{{- if .Values.migrationJob.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: signalchord-migrate-{{ .Release.Revision }}
  labels:
    app.kubernetes.io/name: migration
    app.kubernetes.io/part-of: signalchord
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
    "helm.sh/hook-weight": "-5"
spec:
  backoffLimit: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/name: migration
        app.kubernetes.io/part-of: signalchord
    spec:
      restartPolicy: Never
      serviceAccountName: {{ include "signalchord.workloadServiceAccountName" (dict "root" . "name" "migration") }}
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: migrate
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.migrationJob.image }}:{{ .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          command: {{ toJson .Values.migrationJob.command }}
          env:
            - {name: RAILS_ENV, value: production}
            - {name: SIGNALCHORD_ENV, value: {{ .Values.global.environment | quote }}}
            - {name: KAFKA_BROKERS, value: {{ .Values.global.config.kafkaBrokers | quote }}}
            - {name: KAFKA_TLS_ENABLED, value: {{ .Values.global.config.kafkaTlsEnabled | quote }}}
            - {name: KAFKA_SASL_ENABLED, value: {{ .Values.global.config.kafkaSaslEnabled | quote }}}
            - {name: REDIS_URL, value: {{ .Values.global.config.redisUrl | quote }}}
            - {name: OPENSEARCH_URL, value: {{ .Values.global.config.opensearchUrl | quote }}}
            - {name: WEB_ORIGINS, value: {{ .Values.global.config.webOrigin | quote }}}
            - {name: DISABLE_BOOTSNAP, value: "true"}
            - {name: TMPDIR, value: /tmp}
          envFrom:
            - secretRef: {name: {{ .Values.global.existingSecret }}}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          volumeMounts:
            - {name: tmp, mountPath: /tmp}
      volumes:
        - name: tmp
          emptyDir: {}
{{- end }}
