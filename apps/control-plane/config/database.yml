default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>
  url: <%= ENV["DATABASE_URL"] %>

development:
  <<: *default
  database: signalchord_control_plane_development

test:
  <<: *default
  database: signalchord_control_plane_test

production:
  <<: *default
