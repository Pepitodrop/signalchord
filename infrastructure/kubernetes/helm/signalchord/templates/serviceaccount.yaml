{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "signalchord.serviceAccountName" . }}
  labels: {app.kubernetes.io/part-of: signalchord}
automountServiceAccountToken: false
{{- end }}
